image: python:3.12

stages:
  - test

before_script:
  - python --version
  - pip install ruff bandit

lint:
  stage: test
  script:
    - ruff check .

security:
  stage: test
  script:
    - bandit -r .